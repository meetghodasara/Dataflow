## Configuration of the application properties for source and destination URL 

  1. transfer.source.url = {source-url}
  2. transfer.destination.url = {destination-url}
  3. transfer.interval = {time-interval-in-miliseconds}
